How to use the API

Although it might seem like it (because of the package name being *.api), you shouldn't copy the API package to your mod.
Instead, start up the mod with 'debug-mode' enabled in the config, and read the pages beginning with 'dev:'